[URL to run when docker container](http://localhost:8000/indexes/)

This is the simplest way to run a NON_DOCKERIZED Python application inside the virtual environment:
```sh
pipenv run python src/main.py
```
